# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# UV
#   Similar to Pipfile.lock, it is generally recommended to include uv.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#uv.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/latest/usage/project/#working-with-version-control
.pdm.toml
.pdm-python
.pdm-build/

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Ruff stuff:
.ruff_cache/

# PyPI configuration file
.pypirc
# 📦 Игнорируем папку с байт-кодом Python
__pycache__/        # Python автоматически компилирует .py → .pyc; эти файлы не нужны в Git (иначе будут засорять репозиторий)

# 📦 Игнорируем байткод и скомпилированные файлы Python
*.py[cod]           # Файлы: .pyc, .pyo, .pyd — скомпилированный код (иначе будешь коммитить временные артефакты)
# Что такое *.py[cod]: - #Это шаблон, который игнорирует не .py, а все скомпилированные производные от .py:
           #*.pyc — байткод Python (основной формат)
           #*.pyo — байткод с оптимизациями (устарело, но иногда встречается)
           #*.pyd — бинарный модуль для Windows (по сути DLL)

# 📦 Игнорируем временные классы, которые иногда генерирует Python
*$py.class          # Редко используется, но может появиться в некоторых сборках (иначе лишние файлы в Git)

# 🧠 Игнорируем конфигурации среды разработки PyCharm
.idea/              # PyCharm хранит тут настройки проекта (иначе конфликты настроек между разработчиками)

# 🧠 Игнорируем отдельные конфигурационные файлы JetBrains
*.iml               # Конфигурации модулей PyCharm/IntelliJ (иначе будут попадать в коммиты)

# 🧪 Игнорируем виртуальное окружение Python
venv/               # Виртуальное окружение с библиотеками (иначе случайно vможно залить много лишнего и зависимостей)

# 🔐 Игнорируем переменные окружения и чувствительные данные
.env                # Файл с секретами (иначе можно случайно слить пароли, ключи и настройки)
*.env               # Расширение на случай других .env-файлов (.env.prod, .env.dev и т.п.)

# 🍎 Игнорируем мета-файл macOS
.DS_Store           # Создаётся на Mac в каждой папке (иначе будет мешаться, если используется Mac)

# 🪟 Игнорируем системный файл Windows
Thumbs.db           # Автоматически создаётся Windows (иначе попадёт в репу, если работать с Win)

# 📝 Игнорируем логи и PID-файлы
*.log               # Лог-файлы — временные (иначе можно закоммитить логи работы сервера)
*.pid               # Хранят ID процесса — тоже временные (иначе бесполезны в Git)

# 🗃️ Игнорируем временные БД SQLite
*.sqlite3           # Если будет использоваться SQLite для теста (иначе зальются данные)
*.db                # Общее правило на случай других БД-файлов

# 💡 Игнорируем swap-файлы редакторов
*.swp               # Используется Vim при редактировании (иначе попадут случайно в коммит)
*.swo               # Аналогично — временные редакторские файлы

# 🔙 Игнорируем временные и резервные файлы
*~                  # Создаются при автосохранении или копировании (иначе будет мусор)

# 🧪 Игнорируем артефакты тестирования и покрытия кода
.coverage           # Покрытие тестами (иначе лишние тест-отчёты в Git)
htmlcov/            # Папка с html-отчётами покрытия (иначе лишний вес)
.cache/             # Кэш pytest/coverage (иначе лишние данные от тестов)
.tox/               # tox — утилита для тестирования в разных средах (иначе ненужные артефакты)
pytest_cache/       # Кэш pytest — чтобы тесты шли быстрее (иначе бесполезно в Git)

# 📦 Игнорируем Node.js зависимости
node_modules/       # Если когда-нибудь будет подключен NPM (иначе > 1000 файлов может попасть в Git)

# 📂 Игнорируем временные или служебные папки (если будут использоваться)
uploads/            # Например, если пользователи загружают файлы (иначе можно закоммитить чужие данные)
tmp/                # Временные файлы, кеши, отчёты (иначе они будут сбивать ревизии)
